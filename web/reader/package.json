{
  "name": "@liyasa/reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT OR Apache-2.0",
  "description": "The reader runtime: framework-free progressive enhancement for the default theme (PRD \u00a711).",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node --disable-warning=ExperimentalWarning build.mjs",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.ts",
    "serve": "node e2e/serve.mjs",
    "e2e": "playwright test --config e2e/playwright.config.ts",
    "site": "cd ../.. && cargo run -q -p liyasa-tests --bin reference-site -- target/reference-site"
  },
  "devDependencies": {}
}
