---
title: E0614
description: "`verify-chain` steps cannot run as one chain"
sidebarTitle: E0614
---

# E0614

**`verify-chain` steps cannot run as one chain**

| | |
|---|---|
| Severity | Error |
| Area | Verification and facts |
| Raised by | `liyasa-verify` |

`verify-chain` runs a `steps` block's code in one sandbox, in order, so step
two can use what step one left behind. That needs one process, and these steps
do not share a language — or one of them is not a code block at all.

## Fix it

Give every step the same language, or drop `verify-chain` and let each block be
its own check:

````markdown
:::steps verify-chain
```bash verify
export TOKEN=abc
```

```bash verify expect="abc"
echo "$TOKEN"
```
:::
````

Blocks in a `code-group` or `tabs` are always verified independently; only
`steps` may chain.

Related: [E0609](E0609.md) is a `verify` attribute Liyasa could not read.

## Getting help

[Toolchain and sandbox setup](/help/sandbox) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
