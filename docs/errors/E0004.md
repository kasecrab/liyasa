---
title: E0004
description: "Sandbox (Docker or Podman) required and not available"
sidebarTitle: E0004
---

# E0004

**Sandbox (Docker or Podman) required and not available**

| | |
|---|---|
| Severity | Error |
| Area | CLI and I/O |
| Raised by | `liyasa-cli` |

This diagnostic comes from `liyasa-cli`. The message carries the file, the line, and the value that caused it; run the command again with `--json` to get it as structured output.

## Getting help

[Toolchain and sandbox setup](/help/sandbox) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
