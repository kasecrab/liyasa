---
title: E0213
description: "`link()` or `page()` names a page that does not exist"
sidebarTitle: E0213
---

# E0213

**`link()` or `page()` names a page that does not exist**

| | |
|---|---|
| Severity | Error |
| Area | Templating and Source Document |
| Raised by | `liyasa-markdown` |

:::info{title="This page is not written yet"}
`E0213` is registered and raised by `liyasa-markdown`, and everything above is generated from the registry. What causes it and how to fix it has not been written yet.

The message itself carries the file, the line, and the value that caused it; `--json` gives you the same as structured output.
:::

## Getting help

[Build errors](/help/build-errors) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
