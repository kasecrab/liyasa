---
title: E0812
description: "Uploaded asset rejected (type not allowed or SVG sanitization failed)"
sidebarTitle: E0812
---

# E0812

**Uploaded asset rejected (type not allowed or SVG sanitization failed)**

| | |
|---|---|
| Severity | Error |
| Area | Server, auth, deployments, network |
| Raised by | `liyasa-server` |

:::info{title="This page is not written yet"}
`E0812` is registered and raised by `liyasa-server`, and everything above is generated from the registry. What causes it and how to fix it has not been written yet.

The message itself carries the file, the line, and the value that caused it; `--json` gives you the same as structured output.
:::

## Getting help

[Domains, certificates, and deployments](/help/domains) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
