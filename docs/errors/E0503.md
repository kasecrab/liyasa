---
title: E0503
description: "Spec source unreachable (with DNS, TLS, or policy detail)"
sidebarTitle: E0503
---

# E0503

**Spec source unreachable (with DNS, TLS, or policy detail)**

| | |
|---|---|
| Severity | Error |
| Area | OpenAPI and API docs |
| Raised by | `liyasa-openapi` |

This diagnostic comes from `liyasa-openapi`. The message carries the file, the line, and the value that caused it; run the command again with `--json` to get it as structured output.

## Getting help

[Build errors](/help/build-errors) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
