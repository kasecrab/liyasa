---
title: W0723
description: "Pages in a reserved directory are not routed"
sidebarTitle: W0723
---

# W0723

**Pages in a reserved directory are not routed**

| | |
|---|---|
| Severity | Warning |
| Area | Build, cache, assets, determinism |
| Raised by | `liyasa-build` |

A directory the build reserves for something other than content holds files with
a page extension, and those files are not served anywhere: no route, no entry in
the navigation or the sitemap, nothing in `dist/`.

## Fix it

Move the file under a content directory:

```sh
git mv snippets/pricing.md guides/pricing.md
```

The reserved names are `snippets/`, `assets/`, `components/`, `facts/` and
`theme/`, at any depth. Each holds something the build consumes rather than
serves — a snippet is pulled into a page by `{% snippet "pricing" %}`, an asset
is copied to `dist/` as a file — so a `.md` sitting there is content the site
never shows.

A path segment starting with `_` is withheld by the same rule and is not
reported. The underscore is something you typed; a reserved name is an ordinary
word you can choose without knowing it was taken. If you want a directory kept
out of the site and out of this warning, name it `_notes/` rather than
`notes/`.

If the file is meant to be included rather than served, nothing is wrong and the
warning is the confirmation: leave it where it is.

Related: [`E0403`](E0403) for an asset a page names and the build does not have,
and [`W0130`](W0130) for a page that is served but no navigation reaches.

## Getting help

[Build errors](/help/build-errors) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
