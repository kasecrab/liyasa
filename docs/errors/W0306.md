---
title: W0306
description: "Heading level skipped"
sidebarTitle: W0306
---

# W0306

**Heading level skipped**

| | |
|---|---|
| Severity | Warning |
| Area | Markdown, directives |
| Raised by | `liyasa-markdown` |

This diagnostic comes from `liyasa-markdown`. The message carries the file, the line, and the value that caused it; run the command again with `--json` to get it as structured output.

## Getting help

[Build errors](/help/build-errors) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
