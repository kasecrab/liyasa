---
title: E0407
description: "Generated agent resource declares its continuation as a trailing note instead of an opening header"
sidebarTitle: E0407
---

# E0407

**Generated agent resource declares its continuation as a trailing note instead of an opening header**

| | |
|---|---|
| Severity | Error |
| Area | Navigation and links |
| Raised by | `liyasa-build` |

:::info{title="This page is not written yet"}
`E0407` is registered and raised by `liyasa-build`, and everything above is generated from the registry. What causes it and how to fix it has not been written yet.

The message itself carries the file, the line, and the value that caused it; `--json` gives you the same as structured output.
:::

## Getting help

[Build errors](/help/build-errors) covers the codes in this range, what usually causes them, and what to try first.

Every code is listed in [the error reference](/errors).
