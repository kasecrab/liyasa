import Foundation

var request = URLRequest(url: URL(string: "https://api.example.com/v1/search?q=red%20widget&limit=10&tags=new")!)
request.httpMethod = "GET"

let (data, _) = try await URLSession.shared.data(for: request)
print(String(data: data, encoding: .utf8) ?? "")
