use reqwest::Client;

let client = Client::new();
let response = client
    .request(reqwest::Method::POST, "https://api.example.com/v1/uploads")
    .body("")
    .send()
    .await?;

println!("{}", response.text().await?);
