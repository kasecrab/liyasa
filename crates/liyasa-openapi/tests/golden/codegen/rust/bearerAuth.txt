use reqwest::Client;

let client = Client::new();
let response = client
    .request(reqwest::Method::GET, "https://api.example.com/v1/me")
    .header("Authorization", "Bearer $ACCESS_TOKEN")
    .send()
    .await?;

println!("{}", response.text().await?);
