use reqwest::Client;

let client = Client::new();
let response = client
    .request(reqwest::Method::GET, "https://api.example.com/v1/keys")
    .header("X-API-Key", "$API_KEY")
    .send()
    .await?;

println!("{}", response.text().await?);
