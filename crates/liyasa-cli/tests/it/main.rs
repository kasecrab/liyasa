//! One test binary for the crate (RFC 0007). Every acceptance test is a module
//! here; add a `mod` line for a new file.

mod support;

mod cli_04_validate;
mod cli_30_diagnostics;
mod cli_31_exit;
mod cli_34;
