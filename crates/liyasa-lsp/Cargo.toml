[package]
name = "liyasa-lsp"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The Liyasa Markdown language server: diagnostics, completion, hover, definition, and preview over the Source Document (PRD §15.7 ED-61, §16.1 CLI-25)."

[lints]
workspace = true

[dependencies]
liyasa-core = { path = "../liyasa-core", version = "0.1.0" }
liyasa-markdown = { path = "../liyasa-markdown", version = "0.1.0" }
liyasa-components = { path = "../liyasa-components", version = "0.1.0" }
# The workspace reader (`OsVfs`), the in-memory one the tests drive, and
# `SiteConfig` for the variables completion offers.
liyasa-config = { path = "../liyasa-config", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
