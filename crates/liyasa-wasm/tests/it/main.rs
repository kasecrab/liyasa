//! One test binary for the crate. Every test file is a module here; add a
//! `mod` line for a new file rather than a `[[test]]` row (RFC 0007).

mod budget;
mod search;
mod session;
mod typescript;
mod vfs;
