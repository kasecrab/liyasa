//! Tests whose subject is the built site rather than one crate's API.
//!
//! [`site`] renders the reference site the budgets are measured against and
//! the e2e suite drives; [`budget`] is the measurement itself.

pub mod budget;
pub mod site;
